 logging:
     level:
         org:
             springframework:
                 jdbc:
                     support:
                         jdbcTransactionManager: debug
 mybatis:
     configuration:
         log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
         map-underscore-to-camel-case: true
 spring:
     application:
         name: hospitalRegistration
     datasource:
         driver-class-name: com.mysql.cj.jdbc.Driver
         password: root
         url: jdbc:mysql://localhost:3306/hospitalRegistration
         username: root
